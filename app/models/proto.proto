syntax = "proto3";

message Cursor {
    int64 id = 1;
    optional int64 timestamp = 2;
}

message FileCacheMeta {
    bytes data = 1;
    optional int64 expires_at = 2;
}

message UserTokenStruct {
    int64 id = 1;
    bytes token = 2;
}
