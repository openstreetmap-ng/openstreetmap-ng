<div class="navbar-theme btn-group dropdown d-flex d-lg-inline-flex mb-2 mb-lg-0 mx-lg-1" role="group">
    <button class="btn btn-light btn-bg-initial navbar-color dropdown-toggle" type="button"
            data-bs-toggle="dropdown" aria-expanded="false"
            title="{{ t('theme.toggle_theme') }} ({{ t('theme.auto') }})">
        <i class="bi bi-circle-half"></i><span class="d-lg-none ms-2">{{ t('theme.toggle_theme') }}</span>
    </button>
    <ul class="dropdown-menu dropdown-menu-end">
        <li>
            <h6 class="dropdown-item-text">{{ t('theme.toggle_theme') }}</h6>
        </li>
        <li>
            <button class="dropdown-item" type="button" data-bs-theme-value="light">
                <i class="bi bi-sun-fill me-2"></i>{{ t('theme.light') }}
            </button>
        </li>
        <li>
            <button class="dropdown-item" type="button" data-bs-theme-value="dark">
                <i class="bi bi-moon-stars-fill me-2"></i>{{ t('theme.dark') }}
            </button>
        </li>
        <li>
            <button class="dropdown-item active" type="button" data-bs-theme-value="auto">
                <i class="bi bi-circle-half me-2"></i>{{ t('theme.auto') }}
            </button>
        </li>
    </ul>
</div>
